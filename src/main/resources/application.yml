server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://db.jwenpcugkcmbrvounvbk.supabase.co:5432/postgres?user=postgres
    username: postgres
    password: sPUBWigpInjYpxJl
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
